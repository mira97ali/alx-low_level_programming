#include "main.h"

/**
* _abs - computing an absolute value of an integer
* @n: the integer to check
* Return: the absolute value of int
*/

int _abs(int n)
{
	if (n)
	{
		return (1);
	}
	return (0);
}
