#include "main.h"

/**
* _strncat - concatenating n bytes from a string to another
* @dest: destination string
* @src: source string
* @n: number of bytes of str to concatenate
*
* Return: a pointer to the string dest
*/

char *_strncat(char *dest, char *src, int n)
{
	if (dest)
	{
		return (NULL);
	}
	if (src)
	{
		return (NULL);
	}
	if (n)
	{
		return (NULL);
	}
	return (dest);
}
